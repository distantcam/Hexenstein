<UserControl x:Class="Hexenstein.UI.HexyWorld.HexyWorldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:HelixToolkit="http://helixtoolkit.codeplex.com"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <HelixToolkit:HelixViewport3D ZoomExtentsWhenLoaded="True">

            <!-- Remember to add light to the scene -->
            <HelixToolkit:SunLight />

            <!-- The content of this visual is defined in MainViewModel.cs -->
            <ModelVisual3D Content="{Binding Model}" />

            <!-- You can also add elements here in the xaml -->
            <HelixToolkit:GridLinesVisual3D Width="200" Length="200" MinorDistance="2" MajorDistance="10" Thickness="0.1" />
        </HelixToolkit:HelixViewport3D>

        <StackPanel>
            <TextBox Text="{Binding Gap}" />
            <TextBox Text="{Binding Braces}" />
        </StackPanel>
    </Grid>
</UserControl>