<UserControl x:Class="Hexenstein.UI.HexyWorld.HexyWorldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:HelixToolkit="http://helixtoolkit.codeplex.com"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <HelixToolkit:HelixViewport3D
            ZoomExtentsWhenLoaded="True"
            ItemsSource="{Binding Visuals}"
        >
        </HelixToolkit:HelixViewport3D>

        <StackPanel>
            <TextBox Text="{Binding Value}" />
            <Slider Value="{Binding Value}" TickFrequency="0.1" IsSnapToTickEnabled="True" Maximum="30" Minimum="-30" />
        </StackPanel>
    </Grid>
</UserControl>